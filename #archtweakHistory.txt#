Feb 2, 2020
set
	PKGEXT='.pkg.tar'
	COMPRESSXZ=(xz -c -z - --threads=$(nproc))
	MAKEFLAGS="-j$(nproc)"
in /etc/makepkg.conf

set firefox
browser.cache.disk.enable true
browser.cache.memory.capacity 36196 (in kb)


